const status = {
    pending: "pending",
    complete: "complete",
    delete: "delete"
}

const slotStatus = {
    apply: "apply",
    waiting: "waiting",
    delete: "delete"
}

module.exports = {
    statusEnum: status,
    slotStatus,
}