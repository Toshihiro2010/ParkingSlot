const status = {
    pending: "pending",
    complete: "complete"
}

const carSize = {
    small: "small",
    medium: "medium",
    large: "large"
}

const slotStatus = {
    apply: "apply",
    waiting: "waiting"
}

const carSizeDatas = [
    "small", "medium", "large"
]


module.exports = {
    statusEnum: status,
    carSizeEnum: carSize,
    slotStatus,
    carSizeDatas
}